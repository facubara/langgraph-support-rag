# Pinned during the build; starter set.
# Core orchestration (used from Day 2)
langgraph
langchain-core

# LLM providers (lazy-imported; only needed for non-mock providers)
google-generativeai

# Backend
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# Vector DB (used from Day 3)
chromadb

# Dev / test
pytest
httpx
